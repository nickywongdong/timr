const defaultRepDuration = Duration(seconds: 3);
const defaultSetCount = 1;
const defaultRestDuration = Duration(seconds: 3);
const defaultRestCount = 1;
const tickIncrement = Duration(milliseconds: 10);